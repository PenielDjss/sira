# Sira Architecture Overview

> Visual guide to understand how Sira works

---

## System Architecture

```mermaid
graph TB
    subgraph "User"
        U[Developer runs: sira create]
    end
    
    subgraph "Sira CLI (sira-cli/sira)"
        CLI[index.ts<br/>CLI Entry Point]
        UI[ui.ts<br/>Design System]
        STACKS[stacks.ts<br/>Template Registry]
        CLONE[clone.ts<br/>Clone Pipeline]
    end
    
    subgraph "GitHub"
        REPO[sira-cli/templates<br/>Template Repository]
        HERMES[react-hermes/<br/>React + Vite + TS]
    end
    
    subgraph "Generated Project"
        PKG[package.json]
        SRC[src/]
        AI1[CLAUDE.md]
        AI2[AGENTS.md]
        AI3[.cursorrules]
    end
    
    U --> CLI
    CLI --> UI
    CLI --> STACKS
    CLI --> CLONE
    CLONE --> REPO
    REPO --> HERMES
    CLONE --> PKG
    CLONE --> SRC
    CLONE --> AI1
    CLONE --> AI2
    CLONE --> AI3
```

---

## CLI Flow

```mermaid
sequenceDiagram
    participant User
    participant CLI as index.ts
    participant UI as ui.ts
    participant Stacks as stacks.ts
    participant Clone as clone.ts
    participant GitHub
    participant FileSystem
    
    User->>CLI: sira create
    CLI->>UI: showLogo()
    UI-->>User: Display Sira logo
    
    CLI->>User: Prompt: Project name?
    User-->>CLI: "my-app"
    
    CLI->>Stacks: Get available templates
    Stacks-->>CLI: [HERMÈS]
    
    CLI->>User: Prompt: Choose template
    User-->>CLI: HERMÈS
    
    CLI->>Clone: cloneTemplate(HERMÈS, "my-app")
    Clone->>GitHub: tiged clone sira-cli/templates/react-hermes
    GitHub-->>Clone: Template files
    
    Clone->>FileSystem: Write template files
    Clone->>FileSystem: Generate CLAUDE.md
    Clone->>FileSystem: Generate AGENTS.md
    Clone->>FileSystem: Generate .cursorrules
    Clone->>FileSystem: Update package.json name
    
    Clone-->>CLI: Success
    CLI->>UI: success("Project created!")
    UI-->>User: Display next steps
```

---

## File Structure

### Sira CLI Repository (sira-cli/sira)

```
sira/
├── src/
│   ├── index.ts          # CLI entry point
│   │   ├── main()        # Parse commands
│   │   ├── createCommand()
│   │   ├── listCommand()
│   │   └── showHelp()
│   │
│   ├── ui.ts             # Design system
│   │   ├── colors        # Color palette
│   │   ├── showLogo()    # ASCII art
│   │   └── ui object     # Helper functions
│   │
│   ├── stacks.ts         # Template registry
│   │   ├── Stack interface
│   │   └── stacks array  # [HERMÈS]
│   │
│   └── clone.ts          # Clone pipeline
│       ├── cloneTemplate()
│       ├── generateAIFiles()
│       └── updatePackageJson()
│
├── package.json          # Dependencies & scripts
├── tsconfig.json         # TypeScript config
├── PROJECT.md            # Project context
├── SPRINT_PLAN.md        # Detailed plan
├── IMPLEMENTATION_GUIDE.md
├── READY_TO_CODE.md
└── README.md             # User documentation
```

### Template Repository (sira-cli/templates)

```
templates/
└── react-hermes/         # HERMÈS template
    ├── package.json      # React + Vite + TS deps
    ├── tsconfig.json     # TS config
    ├── vite.config.ts    # Vite config
    ├── index.html        # Entry HTML
    ├── .gitignore
    ├── src/
    │   ├── main.tsx      # React entry point
    │   ├── App.tsx       # Main component
    │   ├── App.css       # Styles
    │   └── vite-env.d.ts # Vite types
    └── public/
        └── vite.svg      # Logo
```

### Generated Project Structure

```
my-app/                   # User's new project
├── package.json          # Name updated to "my-app"
├── tsconfig.json
├── vite.config.ts
├── index.html
├── .gitignore
├── src/
│   ├── main.tsx
│   ├── App.tsx
│   ├── App.css
│   └── vite-env.d.ts
├── public/
│   └── vite.svg
├── CLAUDE.md             # ✨ Generated by Sira
├── AGENTS.md             # ✨ Generated by Sira
└── .cursorrules          # ✨ Generated by Sira
```

---

## Data Flow

```mermaid
graph LR
    A[User Input] --> B[Project Name]
    A --> C[Template Choice]
    
    B --> D[Validation]
    C --> E[Stack Lookup]
    
    D --> F{Valid?}
    F -->|No| G[Show Error]
    F -->|Yes| H[Clone Process]
    
    E --> H
    
    H --> I[tiged Clone]
    I --> J[Template Files]
    
    H --> K[Generate AI Files]
    K --> L[CLAUDE.md]
    K --> M[AGENTS.md]
    K --> N[.cursorrules]
    
    H --> O[Update package.json]
    
    J --> P[Final Project]
    L --> P
    M --> P
    N --> P
    O --> P
    
    P --> Q[Success Message]
```

---

## Module Dependencies

```mermaid
graph TD
    INDEX[index.ts] --> UI[ui.ts]
    INDEX --> STACKS[stacks.ts]
    INDEX --> CLONE[clone.ts]
    
    CLONE --> STACKS
    
    UI --> PC[picocolors]
    INDEX --> CLACK[@clack/prompts]
    CLONE --> TIGED[tiged]
    CLONE --> FS[fs/promises]
    CLONE --> PATH[path]
```

---

## Tech Stack Layers

```mermaid
graph TB
    subgraph "User Interface"
        CLACK[@clack/prompts<br/>Interactive CLI]
        COLORS[picocolors<br/>Terminal colors]
    end
    
    subgraph "Core Logic"
        TS[TypeScript<br/>Type safety]
        ESM[ESM Modules<br/>Modern imports]
    end
    
    subgraph "Template Management"
        TIGED[tiged<br/>GitHub cloning]
        FS[Node.js fs<br/>File operations]
    end
    
    subgraph "Runtime"
        NODE[Node.js 24+<br/>JavaScript runtime]
    end
    
    CLACK --> TS
    COLORS --> TS
    TS --> ESM
    ESM --> TIGED
    ESM --> FS
    TIGED --> NODE
    FS --> NODE
```

---

## Command Flow

### `sira create`

```mermaid
stateDiagram-v2
    [*] --> ShowLogo
    ShowLogo --> PromptName
    PromptName --> ValidateName
    ValidateName --> PromptTemplate: Valid
    ValidateName --> PromptName: Invalid
    PromptTemplate --> CloneTemplate
    CloneTemplate --> GenerateAI
    GenerateAI --> UpdatePackage
    UpdatePackage --> ShowSuccess
    ShowSuccess --> [*]
    
    PromptName --> Cancelled: Ctrl+C
    PromptTemplate --> Cancelled: Ctrl+C
    Cancelled --> [*]
```

### `sira list`

```mermaid
stateDiagram-v2
    [*] --> ShowLogo
    ShowLogo --> LoadTemplates
    LoadTemplates --> DisplayList
    DisplayList --> [*]
```

---

## Error Handling Strategy

```mermaid
graph TD
    A[User Action] --> B{Input Valid?}
    B -->|No| C[Show Validation Error]
    C --> A
    
    B -->|Yes| D[Execute Command]
    D --> E{Success?}
    
    E -->|Yes| F[Show Success]
    F --> G[Display Next Steps]
    
    E -->|No| H{Error Type?}
    H -->|Network| I[GitHub Connection Error]
    H -->|Permission| J[File System Error]
    H -->|Other| K[Generic Error]
    
    I --> L[Show User-Friendly Message]
    J --> L
    K --> L
    
    L --> M[Exit with Code 1]
```

---

## AI Files Generation Process

```mermaid
graph LR
    A[Template Selected] --> B[Load AI Content]
    B --> C[CLAUDE.md Template]
    B --> D[AGENTS.md Template]
    B --> E[.cursorrules Template]
    
    C --> F[Replace Placeholders]
    D --> F
    E --> F
    
    F --> G[Project Name]
    F --> H[Tech Stack]
    
    G --> I[Write CLAUDE.md]
    H --> I
    
    G --> J[Write AGENTS.md]
    H --> J
    
    G --> K[Write .cursorrules]
    H --> K
    
    I --> L[Generated Project]
    J --> L
    K --> L
```

---

## Development Workflow

```mermaid
graph TB
    A[Write Code] --> B[npm run dev]
    B --> C[tsx Executes]
    C --> D[Test CLI]
    
    D --> E{Works?}
    E -->|No| F[Debug]
    F --> A
    
    E -->|Yes| G[npm run build]
    G --> H[TypeScript Compiles]
    H --> I[dist/ Created]
    
    I --> J[npm link]
    J --> K[Global sira Command]
    K --> L[Test in Real Environment]
    
    L --> M{Ready?}
    M -->|No| A
    M -->|Yes| N[Commit & Push]
```

---

## Key Design Decisions

### Why tiged over degit?
- degit is unmaintained and incompatible with Node 24
- tiged is a maintained fork with same API
- Ensures future compatibility

### Why Pure ESM?
- Modern standard (Node 24 native)
- Better tree-shaking
- Future-proof
- Cleaner syntax

### Why @clack/prompts?
- Beautiful, consistent UI
- Easy to use high-level API
- Built-in cancellation handling
- Modern and maintained

### Why Separate Template Repo?
- Clean separation of concerns
- Easy to add new templates
- Users can fork and customize
- Better version control

### Why Generate AI Files?
- Core value proposition of Sira
- Saves developers time
- Ensures consistency
- Makes projects AI-ready from day one

---

**This architecture is designed to be simple, maintainable, and extensible.** 🚀